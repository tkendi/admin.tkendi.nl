import { styled } from '@/styles/stitches.config';

export const LayoutStyled = styled('div', {
  width: '100%',
  height: '100%',
});

export const LayoutChildrenWrap = styled("div", {
  width: "100%",
  height: "calc(100% - 60px)",
  display: "flex"
})

